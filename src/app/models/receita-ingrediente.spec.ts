import { ReceitaIngrediente } from './receita-ingrediente';

describe('ReceitaIngrediente', () => {
  it('should create an instance', () => {
    expect(new ReceitaIngrediente()).toBeTruthy();
  });
});
